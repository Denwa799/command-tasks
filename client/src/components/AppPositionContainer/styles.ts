import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  horizontal: {
    flexDirection: 'row',
  },
  center: {
    height: '100%',
    justifyContent: 'center',
  },
  horizontalCenter: {
    alignItems: 'center',
  },
  bottom: {
    position: 'absolute',
    bottom: 20,
    width: '100%',
  },
});
